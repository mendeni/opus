name: Opus — Build on Windows

on:
  push:
    branches:
      - gh-actions
  pull_request:

jobs:
  build:
    name: Build (Windows)
    runs-on: windows-latest

    steps:
      # 1) Checkout the repo
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup MSYS2 and install build tools
        uses: msys2/setup-msys2@v2
        with:
          msystem: MINGW64        # or MSYS or MINGW32 depending on your needs
          update: true            # run pacman -Syu (the action handles the restart dance)
          install: >
            base-devel
            autoconf
            automake
            m4
            perl
            pkg-config
            libtool

      - name: Debug workspace & run make (MSYS2)
        shell: msys2 {0}
        run: |
          echo "GITHUB_WORKSPACE: $GITHUB_WORKSPACE"
          echo -n "pwd:" 
          pwd
          echo "################"
          # ls -l /mingw64/share/aclocal
          # ln -s /mingw64/share/aclocal /mingw64/share/aclocal-1.16
          # ls -l /mingw64/share/aclocal-1.16
          # find / -name 'aclocal*' -type f
          echo "################"
          make --version
          autoreconf -fi || true
          ./configure CC="gcc" --enable-deep-plc
          make
          make check

      # 5) Move libopus.a for packaging
      - name: Move libopus.a
        run: |
          mv -v .libs/libopus.a libopus.a

      # 6) Upload libopus.a and headers
      - name: Upload libopus.a and headers
        uses: actions/upload-artifact@v4
        with:
          name: opus-${{ runner.os }}
          path: |
            libopus.a
            include/opus_custom.h
            include/opus_defines.h
            include/opus_multistream.h
            include/opus_projection.h
            include/opus_types.h
            include/opus.h
